"""
Utilities package initialization.

This __init__.py file is intentionally left empty. It serves to mark the
'utils' directory as a Python package, allowing its modules (e.g., `seed_data.py`)
to be imported by other parts of the application.
"""
